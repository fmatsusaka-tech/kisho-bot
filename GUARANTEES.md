# GUARANTEES

## 維持すべき保証

| 保証 | 壊してはいけない理由 | テスト・確認方法 |
|---|---|---|
| `kisho-bot`はOutputから独立し、`teiki-chosa-output`を変更しない | リポジトリの責務分離 | 変更ファイル、remote、外部書込み先をPRで確認 |
| 川辺は平均・最高・最低気温と降水量、湯浅は降水量を扱う | 観測地点ごとのデータ契約 | CSV必須列と公開画面を確認。湯浅の気温列を追加・補完しない |
| 欠測値と実測0を区別し、欠測を0へ変換しない | 降水・積算温度の誤集計防止 | `weather-data.test.ts` と `weather-period.test.ts` |
| 積算温度は `Σ max(日平均気温 − 基準温度, 0)` | 栽培判断に使う計算規則 | 基準3・5・8℃の `weather-period.test.ts` |
| 積算温度の初期基準は5℃、選択肢は3・5・8℃ | 利用者向け仕様 | 公開画面の操作確認。型 `BaseTemperature` を確認 |
| 「30日」はデータ末尾30件、「今年」は最新観測日の年 | 暦日が未入力でも現在の仕様を一貫させる | `weather-period.test.ts` |
| 指定期間は開始日・終了日を含み、開始日が後なら集計しない | 範囲指定の誤解防止 | `weather-period.test.ts` と画面エラー表示 |
| 今年と指定期間は、同じ月日範囲の過去2年まで比較できる | 年度比較の利用者仕様 | `comparisonPeriod`のテストと、公開画面で3年を選んだ色・凡例を確認 |
| 「全部」は最高・平均・最低気温、降水量、積算温度を一枚のグラフに表示する | 表示仕様 | 全部を選び、項目の複数選択と一枚表示を確認 |
| グラフは0.5〜5倍、通常画面と別画面の横移動中も縦軸と単位を確認できる | スマートフォンでの可読性 | 実機または狭いブラウザ幅で縮小・拡大・別画面を操作確認 |
| 出典として気象庁と、まつさか農園による加工・集計を表示する | データ利用の説明責任 | 公開ページのfooterを確認 |
| CSVに必須列がなければ、誤った表示をせず取得失敗にする | 列ずれによる誤表示防止 | `weather-data.test.ts` |
| 重複日、最高最低の逆転、負の湯浅降水量を検知する | 基本的なデータ品質確認 | `weather-data.test.ts` と画面のDATA HEALTH |

## データ更新時に守る業務ルール

以下は現行アプリで未実装ですが、将来の取得・保存処理で必ず維持する要求です。

- 2015年1月1日以降を整備対象とする。
- 日付をキーに更新し、重複行を作らない。
- 毎日6時頃、前日までの直近3日を再取得し、気象庁の訂正を反映する。
- 生データシートに分析値や警告を書き込まない。
- 15日・30日の平均気温と、15日・30日の積算降水量を扱う。
- Output側のSpreadsheetへ書き込まない。

この経路にはまだ自動テストも実動確認方法もありません。実装時に、同日再実行の冪等性、欠測保持、直近3日の訂正、書込み先Spreadsheet IDをテストしてください。

## セキュリティ保証

- 現行の公開画面に認証情報・APIキー・サービスアカウント鍵を埋め込まない。
- 秘密情報をソース、公開CSV、ビルド成果物、ログ、文書へ記載しない。
- Google Spreadsheetは現在、公開CSVとして読めることを前提とする。機密情報を保存しない。
- 将来Writerを実装する場合、認証情報はGitHub Secrets等のサーバー側だけで扱い、ブラウザへ渡さない。
- GitHub Actionsの権限は必要最小限とし、Pages workflowは `contents: read`, `pages: write`, `id-token: write` を基準とする。

## 変更時の最低確認

```text
npm run typecheck
npm run lint
npm test
npm run build
```

画面変更時は、公開CSVを読めること、直近30日が初期表示されること、スマートフォン相当幅でグラフを操作できることも確認します。
